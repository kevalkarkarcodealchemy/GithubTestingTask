import {Dimensions} from 'react-native';
import Color from '../theme/Colors';

export const patternRegex = /^[a-zA-Z0-9_]+$/;
export const {height} = Dimensions.get('screen');
export const GRADIENT_COLORS = [
  Color.Turquoise_Green,
  Color.Bright_Cyan,
  Color.Bright_Blue,
];
